---
const projects = [
  {
    title: "Cinetria",
    description: "Proyecto de final de grado de Desarrollo de Aplicaciones Web, una aplicación web para consultar información de cine además de poder crear reseñas y ver las de otros usuarios.",
    technologies: ["Laravel", "PHP", "Mysql", "JavaScript", "Tailwind CSS", "Vite", "SweetAlert2", "SwiperJS"],
    image: "/cinetriaShow.png", // OJO: elimina "/public" del path si está dentro de "public/"
    github: "https://github.com/milovocker/cinetria",
    demo: "https://cinetria-master-is2xqa.laravel.cloud/",
    featured: true
  },
];
---

<section id="proyectos" class="py-20 bg-gray-50">
  <div class="container mx-auto px-6">
    <div class="max-w-6xl mx-auto">
      <h2 class="text-4xl md:text-5xl font-bold text-center mb-16 animate-on-scroll">
        Mi <span class="gradient-text">último proyecto</span>
      </h2>
      
      <div class={`grid ${projects.length === 1 ? '' : 'md:grid-cols-2 lg:grid-cols-3'} gap-4`}>
        {projects.map((project, index) => (
          <div 
            class={`animate-on-scroll ${projects.length === 1 ? 'w-4/5 mx-auto' : project.featured ? 'md:col-span-2 lg:col-span-1' : ''}`}
          >
            <div class="bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 overflow-hidden">
              <div class="relative overflow-hidden">
                <a href={"" + project.demo} target="_blank" rel="noopener noreferrer">
                  <img 
                    src={project.image} 
                    alt={project.title}
                    class="h-full object-cover transition-transform duration-300 hover:scale-110"
                  />
                </a>
                {project.featured && (
                  <div class="absolute top-4 left-4 bg-primary-600 text-white px-3 py-1 rounded-full text-sm font-medium">
                    Destacado
                  </div>
                )}
              </div>
              
              <div class="p-6">
                <h3 class="text-xl font-bold mb-3 text-dark-800">{project.title}</h3>
                <p class="text-dark-600 mb-4 leading-relaxed">{project.description}</p>
                
                <div class="flex flex-wrap gap-2 mb-6">
                  {project.technologies.map((tech) => (
                    <span class="bg-primary-100 text-primary-700 px-3 py-1 rounded-full text-sm font-medium">
                      {tech}
                    </span>
                  ))}
                </div>
                
                <div class="flex gap-4">
                  <a 
                    href={project.github}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="flex items-center gap-2 bg-dark-800 text-white px-4 py-2 rounded-lg hover:bg-dark-700 transition-colors duration-300"
                  >
                    <!-- GitHub SVG -->
                    GitHub
                  </a>
                  
                  {project.demo !== "#" && (
                    <a 
                      href={project.demo}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="flex items-center gap-2 border-2 border-primary-600 text-primary-600 px-4 py-2 rounded-lg hover:bg-primary-600 hover:text-white transition-all duration-300"
                    >
                      <!-- Demo SVG -->
                      Ver más
                    </a>
                  )}
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>
      
      <div class="text-center mt-12 animate-on-scroll">
        <a 
          href="https://github.com/milovocker"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 bg-primary-600 text-white px-8 py-4 rounded-lg font-medium hover:bg-primary-700 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
        >
          Ver todos mis proyectos en GitHub
        </a>
      </div>
    </div>
  </div>
</section>
