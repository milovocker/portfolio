---
const languages = [
  { code: 'es', label: 'Español', flag: '🇪🇸' }
];
// { code: 'en', label: 'English', flag: '🇬🇧' },
//  { code: 'fr', label: 'Français', flag: '🇫🇷' },
//  { code: 'de', label: 'Deutsch', flag: '🇩🇪' }
---

<header class="w-full bg-neutral-950 border-b border-white/5 shadow-sm">
  <nav class="container mx-auto px-6 py-6 flex items-center justify-between">
    <a href="#inicio" class="text-2xl font-bold text-white tracking-wide">
      Milo Vocker
    </a>
    <!-- Hamburger button (visible on mobile) -->
    <button id="menu-btn" class="md:hidden text-white focus:outline-none" aria-label="Abrir menú">
      <svg width="32" height="32" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M6 10h20M6 16h20M6 22h20"/>
      </svg>
    </button>
    <!-- Nav links -->
    <ul id="nav-links" class="hidden md:flex gap-8 text-zinc-200 font-medium text-lg">
      <li><a id="nav-about" href="#sobre-mi" class="hover:text-indigo-400 transition">Sobre mí</a></li>
      <li><a id="nav-skills" href="#habilidades" class="hover:text-indigo-400 transition">Servicios</a></li>
      <li><a id="nav-contact" href="#contacto" class="hover:text-indigo-400 transition">Contacto</a></li>
    </ul>
    <div class="relative hidden md:block">
      <select
        id="lang-select"
        class="bg-neutral-900 text-white px-4 py-2 rounded-lg border border-white/10 focus:outline-none"
        aria-label="Seleccionar idioma"
      >
        {languages.map(l => (
          <option value={l.code}>{l.flag} {l.label}</option>
        ))}
      </select>
    </div>
  </nav>
  <!-- Mobile menu (hidden by default) -->
  <div id="mobile-menu" class="md:hidden hidden px-6 pb-4">
    <ul class="flex flex-col gap-4 text-zinc-200 font-medium text-lg">
      <li><a id="nav-about-mobile" href="#sobre-mi" class="hover:text-indigo-400 transition">Sobre mí</a></li>
      <li><a id="nav-skills-mobile" href="#habilidades" class="hover:text-indigo-400 transition">Habilidades</a></li>
      <li><a id="nav-contact-mobile" href="#contacto" class="hover:text-indigo-400 transition">Contacto</a></li>
    </ul>
    <div class="mt-4">
      <select
        id="lang-select-mobile"
        class="bg-neutral-900 text-white px-4 py-2 rounded-lg border border-white/10 focus:outline-none w-full"
        aria-label="Seleccionar idioma"
      >
        {languages.map(l => (
          <option value={l.code}>{l.flag} {l.label}</option>
        ))}
      </select>
    </div>
  </div>
</header>

<script>
  // Traducciones
  const translations = {
    es: { about: 'Sobre mí', skills: 'Habilidades', contact: 'Contacto' },
    en: { about: 'About', skills: 'Skills', contact: 'Contact' },
    fr: { about: 'À propos', skills: 'Compétences', contact: 'Contact' },
    de: { about: 'Über mich', skills: 'Fähigkeiten', contact: 'Kontakt' }
  };

  function setLang(lang) {
    document.getElementById('nav-about').textContent = translations[lang].about;
    document.getElementById('nav-skills').textContent = translations[lang].skills;
    document.getElementById('nav-contact').textContent = translations[lang].contact;
    document.getElementById('lang-select').value = lang;

    // Mobile
    document.getElementById('nav-about-mobile').textContent = translations[lang].about;
    document.getElementById('nav-skills-mobile').textContent = translations[lang].skills;
    document.getElementById('nav-contact-mobile').textContent = translations[lang].contact;
    document.getElementById('lang-select-mobile').value = lang;
  }

  let lang = localStorage.getItem('lang') || 'es';
  setLang(lang);

  document.getElementById('lang-select').addEventListener('change', function(e) {
    lang = e.target.value;
    localStorage.setItem('lang', lang);
    setLang(lang);
  });
  document.getElementById('lang-select-mobile').addEventListener('change', function(e) {
    lang = e.target.value;
    localStorage.setItem('lang', lang);
    setLang(lang);
  });

  // Mobile menu toggle
  const menuBtn = document.getElementById('menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  menuBtn.addEventListener('click', () => {
    mobileMenu.classList.toggle('hidden');
  });
</script>