---
export interface Props {
  title: string;
  description?: string;
}

interface PageMeta {
  title: string;
  description: string;
  keywords?: string;
  canonical?: string;
}

const metadata: PageMeta = {
  title: "Milo Vocker | Desarrollo web en Lanzarote",
  description: "Portfolio de Milo Vocker, desarrollador web en Lanzarote.",
  keywords: "desarrollo web, Lanzarote, Milo Vocker, diseño web, páginas web",
  canonical: "https://milovocker.com/"
};
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/mvicon.png" />
    <meta name="description" content={metadata.description} />
    <meta name="keywords" content={metadata.keywords} />
    <link rel="canonical" href={metadata.canonical} />
    <title>{metadata.title}</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="bg-neutral-950 text-neutral-100 font-sans antialiased">
    <slot />
    <script>
      import { gsap } from 'gsap';
      import { ScrollTrigger } from 'gsap/ScrollTrigger';
      gsap.registerPlugin(ScrollTrigger);
      gsap.from('.hero-content', {
        duration: 1,
        y: 50,
        opacity: 0,
        ease: 'power3.out',
        delay: 0.2
      });
      gsap.utils.toArray('.animate-on-scroll').forEach((element) => {
        gsap.from(element, {
          duration: 0.8,
          y: 50,
          opacity: 0,
          ease: 'power2.out',
          scrollTrigger: {
            trigger: element,
            start: 'top 80%',
            toggleActions: 'play none none reverse'
          }
        });
      });
    </script>
  </body>
</html>

<style is:global>
  html {
    scroll-behavior: smooth;
  }
  body {
    line-height: 1.6;
    font-family: 'Montserrat', 'Inter', sans-serif;
    background: #18181b;
    color: #f3f4f6;
  }
  .gradient-text {
    background: linear-gradient(90deg, #e0e7ff 0%, #6366f1 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .glass-effect {
    backdrop-filter: blur(10px);
    background: rgba(24, 24, 27, 0.7);
    border: 1px solid rgba(255, 255, 255, 0.08);
  }
</style>